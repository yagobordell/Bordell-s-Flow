# Documentation

Active documentation is grouped by purpose:

- `architecture/`: system boundaries and shared architecture.
- `components/`: model, workflow and compositor component documentation.
- `operations/`: production operation, deployment and performance guidance.

Historical validation notes and retired implementation handoffs belong in Git history rather than the active documentation tree.
