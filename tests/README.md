# Tests

Tests are grouped by scope:

- `unit/`: isolated application and component behavior.
- `integration/`: pipeline and Salad operational integration behavior.
- `contracts/`: repository, deployment and cross-component contracts.

Run the complete suite with `python -m pytest` from the repository root.
