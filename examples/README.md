# Examples

Versioned sample inputs live here. Runtime inputs belong in `data/input/`, which is intentionally ignored except for `.gitkeep`.

To try a sample with the production runner, copy one of the files from `examples/input/` to `data/input/script.txt` and run the command documented in the repository README.
