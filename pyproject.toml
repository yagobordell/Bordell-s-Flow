[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-video-factory"
version = "0.1.0"
description = "Modular production pipeline for generating AI-assisted videos"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35,<2",
    "openai>=3.2.0,<4",
    "pillow>=10.4",
    "pydantic>=2.8",
    "pydantic-settings>=2.4",
    "python-dotenv>=1.0",
    "psycopg[binary,pool]>=3.2,<4",
]

[project.optional-dependencies]
gpu = [
    "fastapi>=0.116,<1",
    "uvicorn[standard]>=0.35,<1",
]
dev = [
    "fastapi>=0.116,<1",
    "httpx>=0.28,<1",
    "pytest>=8.0",
    "ruff>=0.6",
]

[tool.uv]
required-version = "==0.12.18"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
filterwarnings = [
    "error::DeprecationWarning:ai_video_factory\\..*",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "S101"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]
